import React from 'react';

type TodoLists = {
  id: number;
  text: string;
  title: string;
}[];

const TodoList: React.FC = () => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default TodoList;
